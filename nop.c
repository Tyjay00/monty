#include "monty.h"
/**
 * f_nop- nothing
 * @head: stack head
 * @counter: line_number
 * The f_nop function is used to satisfy the "nop" instruction
 * Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
